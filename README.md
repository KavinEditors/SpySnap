# SpySnap
This program monitors the Downloads folder on Windows and automatically takes a webcam photo whenever a new file is detected. Photos are saved in the Music\SecurityPhotos folder with timestamps, and the script can run silently on system startup.
